import { FirstUpperDirective } from './first-upper.directive';

describe('FirstUpperDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstUpperDirective();
    expect(directive).toBeTruthy();
  });
});
