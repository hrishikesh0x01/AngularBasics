import { MapGenderPipe } from './map-gender.pipe';

describe('MapGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new MapGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
