import { SearchFromArrayOnPipe } from './search-from-array-on.pipe';

describe('SearchFromArrayOnPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFromArrayOnPipe();
    expect(pipe).toBeTruthy();
  });
});
